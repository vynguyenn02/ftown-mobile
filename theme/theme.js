// src/theme/theme.js
export const LightTheme = {
  mode: "light",
  background: "#FFFFFF",       // Trắng tinh khiết
  card: "#F3F4F6",              // Xám nhạt, nhẹ nhàng
  text: "#1A1A1A",              // Đen nhạt hơn để dịu mắt
  subtext: "#6B7280",           // Xám trung tính cho chữ phụ
  primary: "#111111",           // Đen đặc trưng của logo
};

export const DarkTheme = {
  mode: "dark",
  background: "#0B0B0B",       // Gần với nền logo
  card: "#1E1E1E",              // Nổi bật nhẹ nhàng
  text: "#FFFFFF",              // Trắng để nổi bật
  subtext: "#B0B0B0",           // Xám nhạt cho chữ phụ
  primary: "#FFFFFF",           // Logo dùng trắng làm điểm nhấn
};
  